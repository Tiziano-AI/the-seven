{
  "name": "the-seven",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @the-seven/web dev",
    "build": "pnpm --filter @the-seven/web build",
    "start": "pnpm --filter @the-seven/web start",
    "local:doctor": "node --import tsx tools/local-dev.ts doctor",
    "local:bootstrap": "node --import tsx tools/local-dev.ts bootstrap",
    "local:db:up": "node --import tsx tools/local-dev.ts db:up",
    "local:db:down": "node --import tsx tools/local-dev.ts db:down",
    "local:db:reset": "node --import tsx tools/local-dev.ts db:reset",
    "local:dev": "node --import tsx tools/local-dev.ts dev",
    "local:gate": "node --import tsx tools/local-dev.ts gate",
    "local:live": "node --import tsx tools/local-dev.ts live",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "tsc --pretty false --noEmit -p tsconfig.check.json",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:live": "node --import tsx tools/live-test.ts",
    "batch": "node --import tsx apps/cli/src/index.ts",
    "db:bootstrap:check": "node --import tsx tools/db-bootstrap-check.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "@playwright/test": "1.58.2",
    "@the-seven/config": "workspace:*",
    "@the-seven/db": "workspace:*",
    "@types/node": "25.5.0",
    "@types/pg": "8.20.0",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vitest": "4.1.0"
  }
}
